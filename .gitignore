# General
*.DS_Store

# Claude Code files (internal, not for repo consumers)
.claude/
CLAUDE.md
memory/

# Exchange Web Services DLLs (not redistributed — fetched on demand)
Microsoft.Exchange.WebServices.Auth.dll
Microsoft.Exchange.WebServices.Auth.xml
Microsoft.Exchange.WebServices.dll
Microsoft.Exchange.WebServices.xml

# Local scratch files
run*.ps1
instex.ps1

# Temp analysis artefacts (underscore-prefixed scratch work)
_doc2*/
_refdoc*/
_ref_*
_meac*.ps1

# Generated installation documents (EXpress v1.0+ naming: {PC}_EXpress_ExchangeServer-Doc*_{lang}_*.docx)
*_EXpress_ExchangeServer-Doc*.docx
~$*.docx

# Ad-hoc test log files
antispam.txt
word.txt

# Planning docs — kept locally, never committed
docs/ROADMAP.md
docs/plan-*.md
docs/archive/

# Prerequisite downloads cache — populated by tools/Get-EXpressDownloads.ps1
# Not committed: large binaries (UCMA 240 MB, .NET 115 MB) and frequently-updated
# CSS-Exchange scripts. Place your own logo.png in assets/ and commit that instead.
sources/*
!sources/README.md
